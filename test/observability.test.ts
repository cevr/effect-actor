import { describe, it } from "bun:test";

describe("spanMiddleware", () => {
  it.todo("wraps each handler invocation in a span: ActorType(entityId).OperationTag", () => {});
  it.todo("is composable with custom middleware via middleware option array", () => {});
});
