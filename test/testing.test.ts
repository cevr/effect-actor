import { describe, it } from "bun:test";

describe("Actor.testClient", () => {
  it.todo("creates a test client via Entity.makeTestClient", () => {});
  it.todo("call works end-to-end without cluster infrastructure", () => {});
  it.todo("cast returns CastReceipt in test mode", () => {});
  it.todo("peek works against in-memory storage in test mode", () => {});
});
