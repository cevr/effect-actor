---
"effect-encore": minor
---

Add `Actor.withProtocol` for transforming the underlying RpcGroup protocol (middleware, annotations), make `ActorObject` pipeable, and add `PeekResultSchema` generic schema factory for encoding/decoding `PeekResult` values.
